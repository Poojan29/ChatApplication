@model List<ChatApp.Models.Chat>

<h2>User List</h2>
<table>
    <tr><th>Name</th><th>Email</th></tr>
    @* @foreach (var user in Model)
    {
        <tr>
            <td>@user.UserName</td>
            <td>@user.Message</td>
        </tr>
    } *@
</table>
